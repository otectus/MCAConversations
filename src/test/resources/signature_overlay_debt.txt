# Signature say pools with no authored personality overlays (spec 9.3).
#
# EMPTY, and it is meant to stay that way. All five designated tiers are written in all twenty-one
# personality namespaces, in both locales: trade openers, the line about how a trade was learned,
# rebuffs, disclosures, commitments, and — since the arcs stopped being an undeclared side effect —
# every callback.
#
# The lint fails on any uncovered signature pool that is NOT listed here, so a new signature beat
# fails the build until it is written or named here with a reason, and a line named here fails once
# it has been written. The file can only shrink.
