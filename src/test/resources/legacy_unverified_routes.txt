# Migration debt: say -> next routes that have no beat contract yet (spec section 15, Phase 1).
#
# Every line here is one authored villager line whose meaning is undeclared, so nothing can check
# that the buttons it opens are sensible replies to it. The list may only shrink. Adding a line is
# not a way to ship new content: BeatContractLintTest fails any NEW uncontracted route outright,
# and fails any line here that has since been contracted or deleted.
#
# It began at 985 routes, the whole corpus as of commit 16b5ab9. It is now empty, which is the
# exit criterion for Phase 2: every shipped villager line declares what it means, and every page
# is checked against every line that can open it. Keep it that way.
