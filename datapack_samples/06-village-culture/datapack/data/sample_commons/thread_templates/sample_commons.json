{
  "_comment": "Sample 06. One thread, so the borrowed tool can be picked back up. resume_scenes is required: the scene named here is what the player gets on return, instead of the button page they left open.",
  "threads": {
    "commons.borrowed_tool": {
      "topic": "village",
      "subject": "village.commons.borrowed_tool",
      "episode_kind": "commons.borrowed_tool",
      "resume_scenes": ["commons.borrowed_tool.settled"],
      "cooldown_days": 1,
      "expires_after_days": 20,
      "privacy": "ordinary"
    }
  }
}
